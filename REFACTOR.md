Настроена структура проекта, перенесен get_db_connection() на db_helper.session_getter() и перенесен в core/database/db_helper
Перенесены все эндпоинты в api/v1
В сами эндпоинты добавлен проброс сессии с бд через Depends()
Заменил синхронный pymysql -> aiomysql
Написал отдельные crud для get_tasks()-> возвращает все tasks с возможностью задать опциональные параметры(фильтрация) 
ПРОБЛЕМА МИГРАЦИЙ с MySQL: MYSQL не поддерживает транзакционный DDL и если миграция падает на середине, то пройденные шаги применятся к бд!!!(В PostgreSQL такого не произойдет)
вынес логику фильтрации не на фронт а на backend для консистентности данных
хотел навесить HASH index на поля status и priority для быстрой фильтрации, а оказывается в MySQL нет возможности навесить hash index:/
убрал из init.sql логику создания таблиц, вынес это в миграции(в init.sql возможно останется только заполнение mock данными)
реализовал логику отслеживания tasks_changelog и вынес в отдельный сервис в services/tasks




